name: Build

on:
  push:
    branches: [ main ]
    paths-ignore:
      - 'README.md'
      - '.idea/**'
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v6

      - name: Run checks
        run: ./kotlin check
